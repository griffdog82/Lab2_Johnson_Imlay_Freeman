@* @page 
@model Lab2_Johnson_Imlay_Freeman.Pages.Admin.BusinessPartners.AddBusinessPartnerModel
@{
    ViewData["Title"] = "Add New Business Partner";
}

<h2>Add New Business Partner</h2>

<form method="post">
    <label for="Name">Business Partner Name:</label>
    <input type="text" asp-for="Name" class="form-control" required />
    <span asp-validation-for="Name" class="text-danger"></span>

    <form method="post">
        <div class="mb-3">
            <label for="OrgType" class="form-label">Organization Classification:</label>
            <select asp-for="OrgType" class="form-control" required>
                <option value="">Select an option</option>
                <option value="Non-Profit">Non-Profit Organization</option>
                <option value="For-Profit">For-Profit Enterprise</option>
            </select>
            <span asp-validation-for="OrgType" class="text-danger"></span>
        </div>

        <div class="mb-3">
            <label for="BusinessType" class="form-label">Industry Sector:</label>
            <select asp-for="BusinessType" class="form-control" required>
                <option value="">Select an option</option>
                <option value="Technology">Technology</option>
                <option value="Healthcare">Healthcare</option>
                <option value="Finance">Finance</option>
                <option value="Retail">Retail</option>
            </select>
            <span asp-validation-for="BusinessType" class="text-danger"></span>
        </div>

        <div class="mb-3">
            <label for="StatusFlag" class="form-label">Application Status:</label>
            <select asp-for="StatusFlag" class="form-control" required>
                <option value="">Select an option</option>
                <option value="Under Review">Under Review</option>
                <option value="Accepted">Accepted</option>
                <option value="Denied">Denied</option>
            </select>
            <span asp-validation-for="StatusFlag" class="text-danger"></span>
        </div>

        <button type="submit" class="btn btn-primary">Submit</button>
    </form>


    <!-- Dropdown for Active Status (1 = Active, 0 = Inactive) -->
    <label for="ActiveStatus">Active Status:</label>
    <select asp-for="ActiveStatus" class="form-control">
        <option value="1" selected>Active</option>
        <option value="0">Inactive</option>
    </select>
    <span asp-validation-for="ActiveStatus" class="text-danger"></span>

    <br />

    <button type="submit" class="btn btn-primary">Add Business Partner</button>
    <button type="reset" class="btn btn-secondary">Clear</button>
</form>

@if (!string.IsNullOrEmpty(Model.Message))
{
    <p class="alert alert-info">@Model.Message</p>
}
 *@

    @page
<!-- Imported from Lab1 by Griffin Imlay on 2_27_2025 -->
@model Lab2_Johnson_Imlay_Freeman.Pages.Admin.BusinessPartners.AddBusinessPartnerModel
@{
    ViewData["Title"] = "Add New Business Partner";
}

<h2>Add New Business Partner</h2>

<form method="post">
    <div class="mb-3">
        <label for="Name" class="form-label">Business Partner Name:</label>
        <input type="text" asp-for="Name" class="form-control" required />
        <span asp-validation-for="Name" class="text-danger"></span>
    </div>

    <div class="mb-3">
        <label for="OrgType" class="form-label">Organization Classification:</label>
        <select asp-for="OrgType" class="form-control" required>
            <option value="">Select an option</option>
            <option value="Non-Profit">Non-Profit Organization</option>
            <option value="For-Profit">For-Profit Enterprise</option>
        </select>
        <span asp-validation-for="OrgType" class="text-danger"></span>
    </div>

    <div class="mb-3">
        <label for="BusinessType" class="form-label">Industry Sector:</label>
        <select asp-for="BusinessType" class="form-control" required>
            <option value="">Select an option</option>
            <option value="Technology">Technology</option>
            <option value="Healthcare">Healthcare</option>
            <option value="Finance">Finance</option>
            <option value="Retail">Retail</option>
        </select>
        <span asp-validation-for="BusinessType" class="text-danger"></span>
    </div>

    <div class="mb-3">
        <label for="StatusFlag" class="form-label">Application Status:</label>
        <select asp-for="StatusFlag" class="form-control" required>
            <option value="">Select an option</option>
            <option value="Under Review">Under Review</option>
            <option value="Accepted">Accepted</option>
            <option value="Denied">Denied</option>
        </select>
        <span asp-validation-for="StatusFlag" class="text-danger"></span>
    </div>

    <div class="mb-3">
        <label for="PrimaryContact" class="form-label">Primary Contact:</label>
        <select asp-for="PrimaryContact" class="form-control">
            <option value="">Select a Business Representative</option>
            @foreach (var rep in Model.BusinessReps)
            {
                <option value="@rep.UserID">@rep.FullName</option>
            }
        </select>
        <span asp-validation-for="PrimaryContact" class="text-danger"></span>
    </div>

    <div class="mb-3">
        <label for="ActiveStatus" class="form-label">Active Status:</label>
        <select asp-for="ActiveStatus" class="form-control">
            <option value="1" selected>Active</option>
            <option value="0">Inactive</option>
        </select>
        <span asp-validation-for="ActiveStatus" class="text-danger"></span>
    </div>

    <div class="mt-3">
        <button type="submit" class="btn btn-primary">Add Business Partner</button>
        <button type="reset" class="btn btn-secondary">Clear</button>
    </div>
</form>

@if (!string.IsNullOrEmpty(Model.Message))
{
    <p class="alert alert-info">@Model.Message</p>
}

